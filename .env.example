# 环境变量模板 —— 复制本文件为 `.env`，填好后重启看板即可生效。
# （`.env` 已被 .gitignore 忽略，**不会**进入仓库；本模板可以提交）
#
# ⚠️ 为什么需要它：看板内置的「本地执行器」会 spawn 一个 headless CLI 子进程，
#    该子进程按下面这份环境变量鉴权。若既没有这些变量、又读不到本机登录凭据，
#    任务会失败并报 `Authentication required. Please use /login command to sign in`。
#
# ── 凭证（**建议只配一个**，见下）─────────────────────────────────────
#   CODEBUDDY_API_KEY     平台签发的 API Key（推荐：长期有效，不随桌面端登录状态变化）
#   CODEBUDDY_AUTH_TOKEN  既有 OAuth 令牌（会过期，需定期更换）
#
# ⚠️ 这两个**不是「只能配一个」，但也不是随便配两个** —— 已读 CLI 源码核实：
#   CLI 内部按 AuthenticationStoragePriority 竞速（Disabled=0 / Low=1 / Normal=5 / Heigh=9），
#   「有 API Key」与「有 Auth Token」都会拿到 Heigh，而 Auth Token 的命中路径会返回
#   **Heigh + 1**（权重更高）。⇒ 两个都配时**谁生效不可预期**，且报错不会指向这里。
#   建议：**只保留一个**。另有 CODEBUDDY_API_KEY_DISABLED 可单独关掉 Key 通道。
#
# ── API Key 获取位置（按你账号所在站点选一个）────────────────────────
#   国内站   https://copilot.tencent.com/profile/
#   国际站   https://www.codebuddy.ai/profile/keys
#   iOA      https://tencent.sso.copilot.tencent.com/profile/keys
#   ⚠️ Key **只在创建时显示一次**，务必立刻复制保存。
#
# ── 站点（不要漏！）────────────────────────────────────────────────
# 官方文档明确写着：`CODEBUDDY_INTERNET_ENVIRONMENT` 是**最常被遗漏**的一项，
# 未设置或设错会导致「鉴权失败或连到错误的服务端点」。
#   国内站 → internal     ← 下面已默认生效（本机桌面端凭据即国内站 www.workbuddy.cn）
#   国际站 → 删掉这一行（用默认值）
#   iOA   → ioa
CODEBUDDY_INTERNET_ENVIRONMENT=internal

# ── 自定义端点（仅专有版 / 自建版需要）─────────────────────────────────
# ⚠️ 它的优先级**高于站点**：一旦填写，请求会被指向该端点，
#    CODEBUDDY_INTERNET_ENVIRONMENT 不再决定路由（官方也把它作为站点解析
#    不匹配时的 workaround）。不是自建版请保持留空。
# CODEBUDDY_BASE_URL=

# ── 取消下面一行的注释并填入你的 Key ─────────────────────────────────
# CODEBUDDY_API_KEY=

# ── 补充：站点与凭证是「绑定」关系，不需要分国内/国外各配一份 ──────────
#   站点（INTERNET_ENVIRONMENT）决定连哪个服务端；凭证是**站点签发**的。
#   所以换站点 = 换凭证，二者不能并行存在 ⇒ 只维护当前这一套即可。
#
# 💡 更省事的做法：直接用设置页的「方式二：CodeBuddy CLI 登录」，
#    凭据由 CLI 自己管理（含过期刷新），无需手写这份文件。
#    两种方式**二选一**即可；都可用时以环境变量为准。
