# 更新日志

本文件记录**面向使用的功能变化**。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 安全

- 后端默认**只监听本机**（`127.0.0.1`），并校验请求来源（`Host` / `Origin`）。
  此前会监听所有网卡且没有任何鉴权，同一局域网内的其它机器可以访问全部接口
- 读取宿主数据的接口对路径参数加了安全校验，修掉了可以越权读取宿主目录之外文件的路径穿越
- 接口的错误信息不再原样回显本机绝对路径（统一收敛为「…\文件名」）

### 修复

- **定期循环任务点「终止」后会被记成「执行完成」**，而且下一轮照常排期。
  现在会正确落成「已取消」，并停止继续排下一轮
- **打开一个还没有消息的会话会持续重复请求**（实测每秒数百次，会让整页反复重渲染）。
  现在只请求一次
- 任务操作的提示不再「假成功」：此前无论成功失败都会弹「已取消执行」之类的成功提示
- 新建任务失败时不再关闭对话框 —— 已填的内容会保留，可以直接改完重试
- 工作空间管理：创建 / 保存失败时不再清空输入并谎报成功；编辑时补上名称与路径的必填校验
- 新建任务对话框现在每次打开都会重置全部选项（此前会沿用上一次的循环配置，
  容易出现「明明没设循环，任务却按周期跑」）
- 工作空间同步的「改挂任务 + 删除原空间」改为原子操作，避免中途失败留下半成品

### 改进

- **「等人授权」的任务不再占用并发槽位** —— 此前几个任务同时卡在等授权，会让整台机器停止调度
- 执行历史里不再堆积「永不完结的执行记录」（任务回退到待办时现在会正确收尾）
- 新建任务对话框在关闭后会销毁内部状态，减少后台无谓的重渲染
- 移除若干无用代码

### 开发

- CI 增加了 3 个此前从未被执行过的验收脚本（看板归类规则、循环接口与集成冒烟）
- 提交前检查在找不到 Node 时改为**拒绝提交**（此前是静默跳过，等于门禁失效）；
  CI 另外增加一次全量误提交扫描
- 发布流程改用仓库的 Node 版本文件，并补上 lockfile 守卫与类型检查
- 两个静态守卫的覆盖范围增强：宿主只读扫描改为递归并覆盖更多扩展名；
  hook 顺序检查能识别更多组件写法

### 文档

- 修正 README「快速开始」中的端口说明：`start.cmd` 的端口取自 `config/port.txt`
  （默认固定 `47831`），不再是自动挑选；补上允许漂移的开关与漂移会使宿主面板入口失效的提示
- 「环境变量」拆为「写入 `.env`」与「启动脚本用的 shell 变量」两张表，并补齐
  `KANBAN_ALLOW_PORT_SHIFT` / `KANBAN_NO_BROWSER` / `KANBAN_NODE` 的说明

## [1.0.0] - 2026-09-15

首个公开版本。

### 核心能力

- **四列任务看板**：待办 / 进行中 / 待决策 / 自动化定时，卡片可拖拽跨列流转
- **工作空间互锁**：同一工作空间内任务串行执行，避免同目录文件相互覆盖
- **优先级与依赖**：任务可设优先级、前置依赖、定时启动时间；依赖未满足不调度
- **全局槽位限制**：跨工作空间的总并发上限可配，避免一次性压满机器
- **实时流式日志**：任务执行输出通过 SSE 推送到卡片，并可展开查看完整会话上下文
- **待决策挂起**：Agent 遇到需要人工确认的操作时自动挂起，由人在看板上确认后继续
- **定时任务**：到点自动进入待办队列；支持周期 / 间隔两档重复、有效期与次数上限
- **工作空间对齐**：一键比对两侧工作空间差异，支持导入与移除

### 调度策略（WSML-P）

每 3 秒一个调度 tick，按 **依赖 → 空间互锁 → 全局槽位** 的顺序判定：
先提升到点的定时任务，再按优先级降序扫描待办，逐个检查依赖是否满足、
同工作空间是否已有任务在跑、全局是否还有空槽，全部通过才启动执行。

### 执行

- 内置**本地执行器**：通过 Agent SDK 在本机直接执行任务，支持权限精确拦截与上下文恢复
- 执行隔离采用**工作空间互锁**（同空间串行），不依赖复制目录

### 界面

- 深色监视风格界面，卡片带状态配色与呼吸动效
- 看板列内容超屏时列内独立滚动，不会撑高整页
- 标题栏统计条常驻，实时显示各列数量与终态细分（完成 / 失败 / 取消）
- 工作空间管理、设置页、任务详情抽屉与对话抽屉

### 技术栈

Express 4 + better-sqlite3（后端）· React 18 + Vite 5 + TDesign（前端）· Server-Sent Events（实时同步）

### 许可

Apache License 2.0。第三方归属与致谢见 [NOTICE](NOTICE) 与
README 的「[引用与致谢](README.md#引用与致谢)」章节。
